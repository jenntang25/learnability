<div class = "text-center">
  <h1>Courses#show</h1>
  <p>Find me in app/views/courses/show.html.erb</p>


  <ul class = "list-unstyled">
    <li><%= @course.title %></li>
    <li> <strong> <%= "City: "%> </strong> <%= @course.establishment.city%></li>
    <li><%= @course.establishment.user.email %></li>
    <li> <%= link_to "see establishment", establishment_path(@course, @establishment ) %></li>
    <li><%= link_to "Book it", new_appointment_path(course_id: @course.id) %></li>
  </ul>

  <div>
  <% if @course.photo? %>
  <% @course.photos.each do |photo| %>
  <%= cl_image_tag photo.path, width: 300, height: 200, crop: :fill %>
<% end %>
<% end %>

</div>






  <% @review.each do |review|%>
<ul class = "list-inline">
  <li><%=review.content%></li>
  <li><%=review.stars%></li>
  <li><%=review.appointment.user.email%></li>



  <%end%>
<br>b
  <%= link_to "Back", :back %>



</div>


<%#= link_to establishment_path(@establishment)%>
<%#= link_to "get appointment", new_appointment_path(@course) %>

